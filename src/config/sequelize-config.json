{
  "development": {
    "dialect": "${DB_DIALECT}",
    "host": "${DB_HOST}",
    "username": "${DB_USERNAME}",
    "password": "${DB_PASSWORD}",
    "database": "${DB_NAME}",
    "logging": false
  },
  "test": {
    "dialect": "${DB_DIALECT}",
    "host": "${DB_HOST}",
    "username": "${DB_USERNAME}",
    "password": "${DB_PASSWORD}",
    "database": "${DB_NAME}",
    "logging": false
  },
  "production": {
    "dialect": "${DB_DIALECT}",
    "host": "${DB_HOST}",
    "username": "${DB_USERNAME}",
    "password": "${DB_PASSWORD}",
    "database": "${DB_NAME}",
    "logging": false
  }
}